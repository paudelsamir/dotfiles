# You can make apps auto-start here
# Relevant Hyprland wiki section: https://wiki.hyprland.org/Configuring/Keywords/#executing

# Backup quickshell startup (DISABLED - was causing duplicates)
# exec-once = sleep 3 && if ! pgrep quickshell > /dev/null; then cd ~/.config/quickshell && quickshell -c ii & fi
